# Leaderboard Service

This service is responsible for rendering the leaderboard page on the server side. It fetches the necessary data from the database and renders the HTML pages using a template engine.

## Run Locally
```bash
yarn install
yarn start
```
